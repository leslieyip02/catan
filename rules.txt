{
    "rules": {
        ".read": "auth != null",
        ".write": false,
        "users": {
            "$uid": {
                ".write": "auth != null && auth.uid == $uid"
            }
        },
        "rooms": {
            ".write": "auth != null"
        }
    }
}